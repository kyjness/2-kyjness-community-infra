apiVersion: v1
kind: Service
metadata:
  name: puppytalk-api
  labels:
    app.kubernetes.io/name: puppytalk-api
spec:
  type: ClusterIP
  selector:
    app: puppytalk-api
  ports:
    - name: http
      port: 80
      targetPort: http
