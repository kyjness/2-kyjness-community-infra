# PuppyTalk Infra - 환경 변수 예시
# 복사: cp .env.example .env 후 값을 채우세요. .env는 Git에 올라가지 않습니다.

# Docker Compose - PostgreSQL (db 서비스)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_DB=puppytalk

# Backend (2-kyjness-community-be) - DB 연결 (Compose 내부 서비스명 사용)
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_postgres_password
DB_NAME=puppytalk
WRITER_DB_URL=postgresql+psycopg://user:pass@db:5432/dbname
READER_DB_URL=postgresql+psycopg://user:pass@db:5432/dbname

# Backend - Redis (Compose 내부 redis 서비스)
REDIS_URL=redis://redis:6379
REDIS_MAX_CONNECTIONS=20

# Backend - JWT·인증 (배포 시 반드시 변경)
JWT_SECRET_KEY=change-me-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_SECONDS=900
REFRESH_TOKEN_EXPIRE_DAYS=7
REFRESH_TOKEN_COOKIE_NAME=refresh_token
COOKIE_SECURE=false

# Backend - 서버 (API 공개 기준 URL. 이미지 링크 등 생성 시 사용)
CORS_ORIGINS=http://127.0.0.1:5173,http://localhost:5173
BE_API_URL=http://localhost  

# Backend - DEBUG (로컬 Docker: true, 배포 시: false)
# Backend - Proxy·Trusted Host, 신고·블라인드
DEBUG=true
TRUST_X_FORWARDED_FOR=true
TRUSTED_HOSTS=*
REPORT_BLIND_THRESHOLD=5


MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Rate limit
RATE_LIMIT_WINDOW=60
RATE_LIMIT_MAX_REQUESTS=100
LOGIN_RATE_LIMIT_WINDOW=60
LOGIN_RATE_LIMIT_MAX_ATTEMPTS=5

# 회원가입 이미지
SIGNUP_IMAGE_TOKEN_TTL_SECONDS=3600
SIGNUP_UPLOAD_RATE_LIMIT_WINDOW=3600
SIGNUP_UPLOAD_RATE_LIMIT_MAX=10

# 파일 업로드 (기본 20MB)
MAX_FILE_SIZE=20971520
ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/webp

# Backend - 스토리지 (local | s3). s3 시 MinIO/AWS S3 사용
STORAGE_BACKEND=s3
S3_BUCKET_NAME=puppytalk
S3_ENDPOINT_URL=http://minio:9000
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
AWS_REGION=ap-northeast-2
S3_PUBLIC_BASE_URL=http://localhost:9000/puppytalk 

# 로깅
LOG_LEVEL=INFO
LOG_FILE_PATH=
SLOW_REQUEST_MS=1000

# 신고·블라인드 (Phase 2)
REPORT_BLIND_THRESHOLD=5

# 조회수 Redis dedup 윈도(초, SET NX EX). 0이면 dedup 비활성(매 요청마다 DB +1)
VIEW_CACHE_TTL_SECONDS=0
